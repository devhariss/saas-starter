// Tailwind v4 is CSS-first — this file is kept only as a marker.
// All configuration lives in app/globals.css via @theme, @source, and @custom-variant.
// Safe to delete this file entirely.
import type { Config } from 'tailwindcss';
const config: Config = {};
export default config;
